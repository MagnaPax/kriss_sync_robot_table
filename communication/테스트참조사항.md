
## 스케일 유무

```python
# 서보 1, 2
InputVel1 = float(input("(1)이동할 속도(RPM)를 입력하세요: "))
InputVel2 = float(input("(2)이동할 속도(RPM)를 입력하세요: "))

convert_InputVel1 = InputVel1 * 6   # RPM을 deg/s로 변환하여 입력
convert_InputVel2 = InputVel2 * 6   # RPM을 deg/s로 변환하여 입력
```

```python
# 서보 3
InputPos = float(input("(3)이동할 좌표(deg)를 입력하세요: "))
InputVel = float(input("(3)이동할 속도(deg/s)를 입력하세요: "))
```

## ServoOnlyExecutor 클래스
- Axis1, Axis2 의 입력값(csv), 만들어진 값(create_for_axis), 실제로 들어간 값(move_velocity) 체크
- Axis3        의 입력값(csv), 만들어진 값(create_for_axis), 실제로 들어간 값(move_absolute) 체크


## 새로 만들어진 원본(1224_xxx.py) 확인